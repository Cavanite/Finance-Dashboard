# Finance-Dashboard
Frontend  React (via CDN, no build step) Plain HTML/CSS  Backend  Node.js + Express  Database  PostgreSQL 16  Infrastructure  Docker + Portainer Watchtower (auto-updates) GitHub Actions (planned for CI/CD)
