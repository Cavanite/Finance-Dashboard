name: Deploy Finance Dashboard

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Copy files to container
        run: |
          docker cp app/. finance-dashboard-app-1:/app/
